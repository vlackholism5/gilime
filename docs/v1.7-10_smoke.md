# v1.7-10 Smoke (Retry/Backoff)

1. v1.7-10_schema.sql 적용 후 retry_count 컬럼 존재 확인.
2. (선택) failed 1건 확보: stub 실행 시 의도적 실패 또는 validation으로 failed 존재 확인.
3. `php scripts/run_delivery_outbound_stub.php --limit=200` 실행.
4. 출력에 processed/sent/failed/skipped_backoff 확인.
5. Workbench에서 v1.7-10_validation.sql 실행 — retry_count, status별 카운트, 최근 20건 확인.
