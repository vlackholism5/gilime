# v0.6-23 진행상황 (GPT 전달용)

아래 블록 전체를 복사해 GPT 대화창에 붙여넣고, "다음 지시서 줘" 또는 "v0.6-24 제안해줘" 등 요청하면 됩니다.

---

```
[프로젝트] GILIME_MVP_01 (PHP + MySQL, XAMPP)
[레포] https://github.com/vlackholism5/gilime (main)
[현재] v0.6-23 Release Candidate 단계. 실데이터 import·검증·push까지 완료. Web Gate 브라우저 확인만 미완료.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[완료한 작업 — seoul_bus_stop_master 11461건 이후]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1) 실데이터 Import 실행 완료
   - 스크립트: scripts/import_seoul_bus_stop_master_full.php
   - 결과: Total=11461, Inserted=0, Updated=11461, Skipped=0
   - seoul_bus_stop_master 최종 건수: 11461건
   - PHP PATH 이슈로 사용자가 "c:\xampp\php\php.exe" 절대경로로 실행함.

2) Git push 완료
   - main → origin/main push 성공 (d84c4a6..63d38db)
   - 로컬 untracked: docs/v0.6-23_결과보고.md, docs/v0.6-23_RC_체크리스트.md 등 문서만.

3) v0.6-20 검증 (Cursor에서 PHP 스크립트로 실행)
   - 스크립트: scripts/run_validation_v20_v21.php
   - RowCount=11461
   - StopNameIndex=ix_seoul_stop_name (stop_name)
   - PK=PRIMARY (NON_UNIQUE=0)
   - EXPLAIN exact: type=ref, key=ix_seoul_stop_name
   - EXPLAIN like_prefix: type=range, key=ix_seoul_stop_name
   → 인덱스 사용 정상, 풀스캔 아님.

4) v0.6-21 검증 (동일 스크립트로 실행, doc_id=1, route=R1, latest_job_id=16)
   - (4) alias canonical: rows=3, master_match=0 건 0, min_master_match=1 → 통과
   - (5) alias_text<=2 blocked=3 (기대 0. v0.6-21 게이트 도입 전 등록 데이터로 추정)
   - (7) approved_empty_matched_stop_id=0 → 통과

5) RC 체크리스트 문서
   - docs/v0.6-23_RC_체크리스트.md: 1) push, 2) Import, 3) v0.6-20, 5) v0.6-21 결과 반영됨.
   - 4) Web Gate 회귀(only_unmatched/only_low 토글, LOW 승인 체크박스, alias 차단 2종, live rematch)는 브라우저에서 수동 확인 필요 → 아직 미기입.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[SoT 요약 — 변경 금지]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- latest PARSE_MATCH 스냅샷 기준 후보 표시
- stale 후보 승인/거절 차단 (UI+서버)
- promote는 latest PARSE_MATCH만 허용
- route_stop 스냅샷 누적 (is_active 0/1)
- alias 등록 시 latest 1건만 live rematch
- 추천 canonical은 only_unmatched=1에서만
- LOW 필터(only_low) + Promote 경고(LOW/auto>=30%)
- v0.6-21: LOW 승인 체크박스 필수, alias 등록 시 canonical 존재+alias_text 길이>2 검증
- v0.6-22: shuttle_parse_metrics 저장 + doc.php 노출 (매칭 로직 불변)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[제약]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 새 테이블/페이지 추가 금지(지시 없는 한)
- 매칭 로직(순서/점수/쿼리) 변경 금지
- SQL/Import 실행은 Cursor PC 앱(터미널/Workbench)에서만. GPT는 실행 불가, 결과만 해석·다음 지시 제공.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[다음 단계 후보]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 4) Web Gate 브라우저 확인 후 RC 체크리스트 완료 → v0.6-23 RC 종료 선언
- v0.6-24: 운영 리포트/관리자 UI 정보구조 최소 리팩터 등 다음 기능 제안
- (5) alias_text<=2 기존 3건: 정리(삭제/마스킹) 필요 여부 판단
```

---

위 내용을 GPT에 붙여넣은 뒤, "다음 지시서 줘" 또는 "v0.6-24에서 할 일 제안해줘" 등으로 요청하세요.
