# v0.6-19 작업 요약 (GPT 전달용)

아래 블록 전체를 복사해 GPT에 붙여넣으면 됩니다.

---

```
[작업 완료] v0.6-19 + UI 개선 + 마이그레이션

[v0.6-19: only_low 필터 + Promote 경고]
1. route_review.php에 only_low=1 필터 추가
   - match_method='like_prefix'만 표시
   - only_unmatched=1과 동시 사용 가능 (like_prefix + unmatched)
   - 토글 링크: "LOW만 보기" / "LOW 해제" (only_unmatched와 파라미터 조합 유지)

2. Promote 경고 문구
   - 조건: low_confidence_cnt > 0 AND auto_matched_cnt > 0 AND (low_confidence_cnt / auto_matched_cnt) >= 0.30
   - 표시: 주황색 배경 "⚠️ 주의: like_prefix(LOW) 비중이 높습니다 (X건 / Y건 자동매칭). Promote 전 후보를 재검토하세요."

3. 매칭 로직/SoT 불변
   - 표시·필터·경고만 추가, 매칭 점수·순서·쿼리 변경 없음
   - SQL 없음 (화면 필터만)

[UI 개선 (v0.6-18 작업에서 추가)]
1. 전체 레이아웃: 밝은 회색 배경, 카드 그림자, 헤더 카드형
2. Summary → 카드 그리드 (4개): Total / Approved / Pending / Route Stops
   - 색상 강조: Approved(초록), Pending(주황)
   - 추가 행(latest 스냅샷 시): Auto Matched / Low Confidence(주황) / None Matched / Alias Used
3. Candidates 테이블
   - status 배지: pending(주황), approved(초록), rejected(빨강)
   - 신뢰도 배지: HIGH(초록), MED(파랑), LOW(주황), NONE(회색)
   - 테이블 헤더 배경 변경, 폰트 굵게
4. Action 버튼
   - 세로 카드형 배치 (기존 가로 → 세로)
   - 각 form이 회색 카드로 감싸짐
   - 아이콘 추가: ✓ Approve, ✗ Reject, 🔖 alias 등록
   - 버튼 색상: Approve(파란 배경), Reject·alias(흰 배경+파란 테두리)

[마이그레이션 실행 완료]
- v0.6-8 ~ v0.6-18 통합 마이그레이션 (scripts/php/migrate_v06_8_to_18.php)
- 테이블 생성:
  1. seoul_bus_stop_master: 10건 더미 (강남역, 서울역, 홍대입구역, 사당역, 신촌역 등)
  2. shuttle_stop_alias: 3건 더미 (강남→강남역, 홍대→홍대입구역, 사당→사당역)
  3. shuttle_stop_normalize_rule: 빈 테이블
- 컬럼 추가:
  1. shuttle_stop_candidate: matched_stop_id, matched_stop_name, match_score, match_method
  2. shuttle_doc_job_log: base_job_id, route_label
- promoted_job_id 제거 (shuttle_route_stop)

[테스트 가능한 기능]
1. Quick Search: "강남", "서울" 등 검색 → 더미 데이터 표시
2. only_low=1 필터: Candidates에서 like_prefix만 표시
3. only_unmatched=1 + only_low=1 조합
4. Promote 경고: LOW 비중 30% 이상 시 주황색 경고
5. 자동매칭: PARSE_MATCH 실행 시 seoul_bus_stop_master 기반 매칭
6. alias 등록 → live rematch

[파일 변경]
- public/admin/route_review.php (UI + only_low 필터 + 경고)
- README.md, docs/STATUS_FOR_GPT.md (v0.6-19 반영)
- scripts/php/migrate_v06_8_to_18.php (신규)

[Git 커밋 대기 중]
- v0.6-18: UI 개선
- v0.6-19: only_low 필터 + Promote 경고
```

---

위 내용을 GPT에 붙여넣은 뒤, "다음 작업 지시서 줘" 또는 "v0.6-20에서 ~ 구현해줘" 등 요청하시면 됩니다.
