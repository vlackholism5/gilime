# v1.1-02 작업실행서

## 5줄 요약
1. **route_review**: Approve/Reject 성공 후 `jump_next=1`이면 동일 doc pending 중 다음 노선으로 redirect, 없으면 기존대로 유지.
2. **route_review**: `build_route_review_redirect_query()`의 `$keep`에 `jump_next` 추가.
3. **route_review**: 필터/전체보기 영역에 「검수 후 다음 노선 자동」 토글 링크 추가 (show_advanced=0/1 모두 노출).
4. **route_review**: PENDING>0 이고 Candidates 목록이 비어 있을 때, 테이블 상단에 muted 안내 1줄 출력.
5. **doc.php**: Next Actions Top20 표의 route_label 링크에 `quick_mode=1&show_advanced=0` 항상 포함.

## 브라우저 스모크 1회 결과
- (A) route_review에서 jump_next=1 켠 뒤 Approve 1회 → 다음 노선 이동 또는 동일 노선 유지: **OK**
- (B) no candidates 안내 문구 조건부 노출: **OK**

---
커밋: `feat(v1.1-02): faster review flow (jump_next + hints + quick links)`
