# =========================================================
# GILIME — Token Safety & Operational Data Isolation v1.0
# =========================================================

# ---------------------------------------------------------
# 1. Large data isolation (절대 Git 추적 금지)
# ---------------------------------------------------------

/data/external_big/
/data/runtime/
/public/uploads/

# Legacy data paths (안전장치)
/data/osm/
/data/public/
/data/inbound/
/data/derived/
/data/generated/

# ---------------------------------------------------------
# 2. Allow small dev sample + folder structure 유지
# ---------------------------------------------------------

!/data/sample/
!/data/**/.gitkeep
!/data/**/README.md

# ---------------------------------------------------------
# 3. Local secrets / environment (절대 커밋 금지)
# ---------------------------------------------------------

.env
.env.local
/app/inc/config/config.local.php
/app/inc/config/*.local.php
app/inc/config.safe.php
*.log

# Aiven verify runner output (로컬 실행 결과)
logs/verify_latest.md

# ---------------------------------------------------------
# 4. Composer / vendor
# ---------------------------------------------------------

vendor/
composer.lock

# ---------------------------------------------------------
# 5. Uploaded files (이중 안전장치)
# ---------------------------------------------------------

public/uploads/*.pdf
public/uploads/*.tmp

composer.phar