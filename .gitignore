# 원본 데이터 — 커밋 제외 (SoT: data/README_DATA_DIRS.md)
data/inbound/

# 로컬/환경 (비밀값 포함, 절대 커밋 금지)
.env
.env.local
app/inc/config.local.php
app/inc/config.safe.php
*.log
# Aiven verify runner output (로컬 실행 결과)
logs/verify_latest.md

# Composer dependencies
vendor/
composer.lock

# Uploaded files (PDF, etc.)
public/uploads/*.pdf
public/uploads/*.tmp
